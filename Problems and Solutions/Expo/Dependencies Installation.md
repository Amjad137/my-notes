Instead of using package managers to install dependencies, expo motivates to use expo cli itself
```Bash
npx expo install <package-name> [another-package]
npx expo install react-native-reanimated
npx expo install react-native-gesture-handler react-native-safe-area-context

npx expo install --fix
```